# 🎬 Система бронирования билетов для кинотеатра

## 📋 Оглавление

- [📌 Описание](#-описание)
- [🚀 Стек технологий](#-стек-технологий)
- [🛠️ Подготовка к запуску](#-подготовка-к-запуску)
- [📄 Содержание .env](#-содержание-env)
  - [🔧 Профиль приложения](#-профиль-приложения)
  - [🗄️ Настройки базы данных](#-настройки-базы-данных)
  - [🛡️ JWT-секрет](#-jwt-секрет)
  - [👤 Учетные данные root-пользователя](#-учетные-данные-root-пользователя)
  - [📊 Учетные данные Grafana](#-учетные-данные-grafana)
  - [📊 Учетные данные Java Melody](#-учетные-данные-java-melody)
- [📡 Запуск проекта](#-запуск-проекта)
- [📝 Логирование](#-логирование)
- [📊 Метрики и мониторинг](#-метрики-и-мониторинг)
    - [🔍 Actuator](#-actuator)
    - [📊 Java Melody](#-java-melody)
    - [📊 Prometheus](#-prometheus)
    - [📈 Grafana](#-grafana)
    - [🔍 Компоненты мониторинга](#-компоненты-мониторинга)
- [📐 ER-диаграмма базы данных](#-er-диаграмма-базы-данных)
- [💾 Миграции базы данных](#-миграции-базы-данных)
- [📘 Swagger-документация](#-swagger-документация)
- [🧪 Тестирование](#-тестирование)

## 📌 Описание

Система предоставляет информацию о киносеансах и позволяет бронировать билеты на фильмы.

## 🚀 Стек технологий

- **Язык**: Java 21
- **Сборщик проекта**: Maven
- **Фреймворк**: Spring Boot 3.4.3
- **База данных**: PostgreSQL 17.2
- **Миграции БД**: Liquibase
- **GUI**: Thymeleaf
- **Логирование**: Spring AOP, Logback, Promtail, Loki 
- **Мониторинг**: Prometheus, Grafana, Java Melody
- **Тестирование**: JUnit, AssertJ, Mockito, Testcontainers, Selenium

## 🛠️ Подготовка к запуску

Перед запуском приложения создайте файл `.env` в корневой директории. В качестве примера используйте `.env.example`.

### **Шаги:**

1. **Скопируйте** файл `.env.example`:
   ```sh
   cp .env.example .env
   ```
2. Заполните `.env` своими данными

## 📄 Содержание .env

### 🔧 Профиль приложения

Для указания профиля приложения используется переменная:

```properties
ACTIVE_PROFILE=dev
```

Доступные значения:
- **dev**
- **prod**
- **test**
- **default** (если профиль не указан)

Выбранный профиль влияет на:
- Наполнение БД
- Уровень логирования


### 🗄️ Настройки базы данных

В файле `.env` необходимо указать параметры подключения к PostgreSQL:

```properties
POSTGRES_DB=cinema
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=postgres
```

### 🛡️ JWT-секрет

Поле `JWT_SECRET` в `.env` должно быть закодировано в формате **Base64URL**.

#### ✅ Допустимые символы в Base64URL:
- Буквы: `A–Z`, `a–z`
- Цифры: `0–9`
- Специальные символы: `-`, `_`

#### 🔐 Генерация JWT-секрета:

Чтобы сгенерировать случайный ключ и привести его к формату Base64URL, можно воспользоваться следующей командой (Linux/macOS):

```sh
openssl rand -base64 64 | tr '+/' '-_' | tr -d '='
```

### 👤 Учетные данные root-пользователя

В файле `.env` необходимо указать следующие параметры для создания root-пользователя:

```properties
ROOT_USERNAME=root
ROOT_PASSWORD=your_password
ROOT_EMAIL=email@example.com
```

Эти учетные данные будут использованы при первом запуске приложения для создания администратора системы.

### 📊 Учетные данные Grafana

Для доступа к панели мониторинга Grafana необходимо указать следующие параметры:

```properties
GRAFANA_USERNAME=admin
GRAFANA_PASSWORD=admin
```

Эти учетные данные будут использоваться для входа в веб-интерфейс Grafana.

### 📊 Учетные данные Java Melody

Для доступа к панели мониторинга Java Melody необходимо указать следующие параметры:

```properties
JAVAMELODY_USERNAME=admin
JAVAMELODY_PASSWORD=admin
```

Эти учетные данные будут использоваться для доступа к метрикам и мониторингу приложения через Java Melody.

## 📡 Запуск проекта

Приложение можно запустить двумя способами: вручную или с использованием Docker.

### 🔄 Запуск вручную

При ручном запуске приложение будет использовать локальную базу данных PostgreSQL, указанную в `.env` файле.

#### 📋 Предварительные требования

- **Java 21**: Убедитесь, что установлена Java версии 21 или выше
- **Maven**: Для сборки проекта необходим Apache Maven
- **PostgreSQL 17**: 
  - PostgreSQL должен быть установлен и запущен на вашей системе
  - Создана база данных с именем, указанным в `POSTGRES_DB`
  - Пользователь PostgreSQL имеет необходимые права доступа

#### 🔧 Сборка

```sh
mvn clean install
```

#### 🚀 Запуск приложения

```sh
mvn spring-boot:run
```

### 🐳 Запуск через Docker

Docker позволяет запустить приложение вместе с базой данных в изолированных контейнерах.

#### 📋 Предварительные требования

- Установленный Docker
- Установленный Docker Compose

#### 🚀 Запуск с помощью Docker Compose

1. Соберите образ приложения:
```sh
docker compose build
```

2. Запустите приложение и базу данных:
```sh
docker compose up -d
```

3. Для остановки контейнеров:
```sh
docker compose down
```

4. Для просмотра логов:
```sh
docker compose logs -f
```

После успешного развертывания (любым способом) приложение будет доступно по адресу:

🔗 [http://localhost:8080/](http://localhost:8080/)

## 📝 Логирование

Система логирования построена на основе нескольких компонентов, которые обеспечивают полный цикл обработки логов: от их создания до визуализации и анализа.

### 🔄 Компоненты системы логирования

1. **Spring AOP + Logback**
   - Использование аспектно-ориентированного программирования для автоматического логирования
   - Логирование всех слоев приложения (Controller, Service, Repository)
   - Структурированное JSON-логирование с использованием Logback
   - Автоматическое добавление trace ID для отслеживания запросов
   - Ротация логов с настраиваемыми параметрами хранения

2. **Promtail**
   - Агент для сбора и отправки логов
   - Чтение JSON-логов из файловой системы
   - Извлечение меток (labels) из логов для лучшей категоризации
   - Отправка логов в Loki с поддержкой буферизации и повторных попыток
   - Метрики производительности и длительности запросов

3. **Loki**
   - Система хранения и индексации логов
   - Эффективное хранение с поддержкой сжатия
   - Удержание логов в течение 15 дней
   - Поддержка запросов LogQL для анализа логов
   - Интеграция с Grafana для визуализации

### 📊 Особенности реализации

#### ⚙️ Конфигурация по профилям
- **dev**: Расширенное логирование SQL и веб-запросов для отладки
- **prod**: Оптимизированное логирование для production среды
- **test**: Логирование для тестового окружения только в консоль
- **default**: Базовое логирование только внутренних компонентов системы

#### 🔄 Процесс обработки логов
1. Приложение генерирует структурированные JSON-логи и сохраняет их в директорию `/logs`
2. Promtail собирает логи из файлов
3. Логи отправляются в Loki для хранения
4. Grafana используется для визуализации и анализа логов через предустановленный дашборд `logs-app`

## 📊 Метрики и мониторинг

### 🔍 Actuator

Spring Boot Actuator предоставляет готовые endpoints для мониторинга:

- 📌 Список всех доступных метрик:  
  [http://localhost:8080/actuator/metrics](http://localhost:8080/actuator/metrics)

- 📌 Проверка состояния приложения:  
  [http://localhost:8080/actuator/health](http://localhost:8080/actuator/health)

- 📌 Метрики в формате Prometheus:  
  [http://localhost:8080/actuator/prometheus](http://localhost:8080/actuator/prometheus)

### 📊 Java Melody

Java Melody предоставляет детальный мониторинг Java-приложения в режиме реального времени:

#### 🔑 Доступ к Java Melody

- 📌 Панель мониторинга:  
  [http://localhost:8080/monitoring](http://localhost:8080/monitoring)
- 🔐 Требуется аутентификация (учетные данные из `.env` указанные в файле `.env` в параметрах `JAVAMELODY_USERNAME` и `JAVAMELODY_PASSWORD`)

#### 📊 Основные возможности

- Мониторинг HTTP-запросов и времени отклика
- Использование памяти и CPU
- Статистика SQL-запросов и производительности БД
- Мониторинг Java-потоков
- Системные ошибки и исключения
- Spring бины и их методы


### 📊 Prometheus

Prometheus собирает метрики приложения и хранит их временные ряды. После запуска приложения Prometheus доступен по адресу:

🔗 [http://localhost:9090](http://localhost:9090)

#### ⚙️ Настройка Prometheus

Конфигурация Prometheus находится в файле `prometheus.yml`:

#### 🎯 Проверка целей мониторинга

Для проверки состояния подключенных источников метрик:

1. Откройте [http://localhost:9090/targets](http://localhost:9090/targets)
2. На странице Targets вы увидите:
    - **spring-boot-app**: статус эндпоинта приложения
    - **node-exporter**: статус сборщика системных метрик
    - **postgres-exporter**: статус сборщика PostgreSQL метрик

#### 🔍 Выполнение запросов

Интерфейс запросов доступен по адресу [http://localhost:9090/query](http://localhost:9090/query)

##### 📊 Примеры запросов:

1. **JVM Метрики**
   ```promql
   # Использование heap памяти
   jvm_memory_used_bytes{area="heap"}
   
   # Количество активных потоков
   jvm_threads_live_threads
   ```

2. **HTTP Метрики**
   ```promql
   # Общее количество HTTP запросов
   http_server_requests_seconds_count
   
   # Средняя латентность за 5 минут
   rate(http_server_requests_seconds_sum[5m]) / rate(http_server_requests_seconds_count[5m])
   ```

3. **Системные метрики**
   ```promql
   # Использование CPU
   100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)
   
   # Использование памяти
   (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100
   ```

4. **PostgreSQL метрики**
   ```promql
   # Доступность базы данных PostgreSQL 
   pg_up
   ```

### 📈 Grafana

Grafana предоставляет визуализацию метрик, собранных Prometheus. Доступ к Grafana:

🔗 [http://localhost:3000](http://localhost:3000)

#### 🔐 Аутентификация

Для входа используются учетные данные, указанные в файле `.env` в параметрах `GRAFANA_ADMIN_USER` и `GRAFANA_ADMIN_PASSWORD`.

#### 📁 Структура конфигурации

Конфигурация мониторинга разделена на несколько компонентов:

```
grafana/
├── dashboards/                          # Предустановленные дашборды
│   ├── node-exporter-full.json         # Системные метрики хоста
│   ├── postgresql-database.json        # Метрики PostgreSQL
│   ├── spring-boot-2-1-system-monitor.json  # Метрики Spring Boot
│   └── spring-http-example.json        # HTTP метрики
└── provisioning/
    ├── dashboards/                     # Автоматическая загрузка дашбордов
    └── datasources/                    # Конфигурация источников данных
```

### 🔍 Компоненты мониторинга

В проекте используются следующие компоненты для мониторинга:

1. **Java Melody**
   - Работает автономно, без необходимости в дополнительных компонентах
   - Предоставляет встроенный веб-интерфейс для мониторинга
   - Собирает и отображает метрики непосредственно из приложения

2. **Prometheus**
   - Собирает метрики из endpoint'ов
   - Хранит временные ряды метрик
   - Предоставляет язык запросов PromQL

3. **Spring Boot Actuator**
   - Собирает и предоставляет базовые метрики приложения
   - Является источником метрик для Prometheus

4. **Node Exporter**
   - Собирает и предоставляем системные метрики хоста
   - Является источником метрик для Prometheus

5. **Postgres Exporter**
   - Собирает и предоставляет метрики и PostgreSQL
   - Является источником метрик для Prometheus

6. **Grafana**
   - Визуализирует метрики из Prometheus
   - Предоставляет настраиваемые дашборды
   - Поддерживает алертинг

## 📐 ER-диаграмма базы данных

Для лучшего понимания структуры базы данных и связей между таблицами представлена **ER-диаграмма**:

🔗 [dbdiagram.io](https://dbdiagram.io/d/bookingCinemaTickets-6719f49497a66db9a3194758)

## 💾 Миграции базы данных

Проект использует **Liquibase** для управления миграциями базы данных. Все миграции находятся в директории `src/main/resources/db/migrations/`.

### 📁 Структура миграций:

- `db.changelog-master.xml` - главный файл конфигурации миграций
- `V0.1/` - директория с миграциями версии 0.1
  - `2025-06-16--00-initial-schema.sql` - создание начальной схемы БД
  - `2025-06-18--00-initial-data.sql` - заполнение первоначальными данными
  - `db.changelog-v0.1-cumulative.xml` - кумулятивный changelog версии 0.1

Миграции применяются автоматически при запуске приложения.

## 📘 Swagger-документация

Для просмотра и тестирования REST API доступен интерфейс Swagger UI:

🔗 [http://localhost:8080/swagger-ui/index.html](http://localhost:8080/swagger-ui/index.html)

## 🧪 Тестирование

### 📋 Требования

Для запуска тестов необходимо:

1. **Docker** - должен быть установлен и запущен на вашей системе
2. **Docker Daemon** - должен быть активен во время выполнения тестов

### 🔍 Особенности

- Тесты используют **Testcontainers** для создания изолированного окружения
- Автоматически поднимается контейнер с PostgreSQL для тестовой базы данных
- Тестовая конфигурация находится в `src/test/resources/application-test.properties`

### ▶️ Запуск тестов

```sh
mvn test
```